---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/package.json
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/pnpm-workspace.yaml
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/turbo.json
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/tsconfig.base.json
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/biome.json
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/lefthook.yml
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitleaks.toml
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.nvmrc
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.env.example
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitignore
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/scripts/preflight-windows.ps1
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/vitest.workspace.ts
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/package.json
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/tsconfig.json
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/src/index.ts
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/package.json
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/tsconfig.json
  - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/src/index.ts
autonomous: false
requirements: [FND-01, FND-10]
must_haves:
  truths:
    - "Running `pnpm turbo typecheck` from repo root succeeds with zero errors on a strict TS 5.5+ monorepo rooted at Node 22 LTS"
    - "Attempting `git commit` of a file containing `mx0testkeyabcdef0123456789abcdef` is blocked by the lefthook+gitleaks pre-commit hook before the commit is created"
    - "biome.json uses `lineEnding: \"crlf\"` and `noConsoleLog: \"error\"` so Windows CRLF stays stable and stray console.log cannot land"
    - "No husky or shell-script pre-commit hook exists anywhere in the repo (lefthook-only, so broken Git Bash cannot bypass secret scanning)"
  artifacts:
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/pnpm-workspace.yaml"
      provides: "Workspace declaration for apps/* and packages/*"
      contains: "apps/*"
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/turbo.json"
      provides: "Turbo task graph for build/dev/typecheck/test/lint/smoke"
      contains: "typecheck"
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/tsconfig.base.json"
      provides: "Strict TypeScript base config (strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes)"
      contains: "noUncheckedIndexedAccess"
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/biome.json"
      provides: "Biome 2.3+ lint+format config with CRLF default and noConsoleLog error"
      contains: "\"lineEnding\": \"crlf\""
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/lefthook.yml"
      provides: "Cross-platform pre-commit hooks (gitleaks + biome check + turbo typecheck)"
      contains: "gitleaks"
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitleaks.toml"
      provides: "Custom gitleaks rules for mx0 MEXC keys + Telegram bot tokens + ETH/Solana wallet keys"
      contains: "mexc-access-key"
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.nvmrc"
      provides: "Node version pin to 22"
      contains: "22"
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/package.json"
      provides: "Root package.json with engines.node>=22 <23, packageManager=pnpm@9.12.0, turbo/biome/lefthook/vitest/tsx scripts"
      contains: "pnpm@9"
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/scripts/preflight-windows.ps1"
      provides: "PowerShell preflight probe for Node 22, pnpm 9, gitleaks, Memurai, Windows Credential Manager targets"
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/src/index.ts"
      provides: "Shared TypeScript types scaffold (SecretName, AccountInfo placeholder)"
    - path: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/src/index.ts"
      provides: "Shared Zod schemas scaffold (empty, ready for later packages)"
  key_links:
    - from: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/lefthook.yml"
      to: "gitleaks protect --staged"
      via: "pre-commit command"
      pattern: "gitleaks protect"
    - from: "C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitleaks.toml"
      to: "MEXC mx0 key regex + Telegram bot token regex"
      via: "custom [[rules]] blocks"
      pattern: "mx0\\[A-Za-z0-9\\]"
---

<objective>
Scaffold the pnpm + Turborepo monorepo on Node 22 LTS with TypeScript 5.5+ strict, install lefthook + gitleaks pre-commit hooks with custom MEXC/Telegram rules, and stand up two empty foundational packages (`shared-types`, `shared-schemas`) ready for downstream plans to fill in.

Purpose: Establish the structural spine of the project so every subsequent plan has a consistent workspace, a compile-time type-strict environment, and structurally-enforced secret leakage prevention. Matt's Git Bash is broken (Cygwin fork errors) — tooling must use lefthook (Rust binary) NOT husky.

Output: Root config files (pnpm-workspace.yaml, turbo.json, tsconfig.base.json, biome.json, lefthook.yml, .gitleaks.toml), preflight PowerShell script, and two package skeletons. `pnpm turbo typecheck` must succeed with zero errors. A test commit containing a planted `mx0...` key must be blocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/PROJECT.md
@C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/ROADMAP.md
@C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/STATE.md
@C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/phases/01-foundation/01-RESEARCH.md
@C:/Users/lucid/Desktop/kr8tiv-mexc-bot/CLAUDE.md

<interfaces>
<!-- No prior plans — greenfield. Interfaces this plan CREATES for downstream plans: -->

From packages/shared-types/src/index.ts (this plan creates):
```typescript
// Allow-list union of known secret names — updated as phases add secrets
export type SecretName =
  | "mexc-spot-access"
  | "mexc-spot-secret"
  | "mexc-whitelist-ip"
  | "telegram-bot-token"          // Phase 3
  | "mexc-futures-access"         // Phase 6
  | "mexc-futures-secret";        // Phase 6
```

From packages/shared-schemas/src/index.ts (this plan creates):
```typescript
// Empty re-export barrel. Downstream plans (02, 03, 04) add MEXC schemas,
// SecretName schema, etc.
export {};
```
</interfaces>
</context>

<deep_work_rules>
Every task MUST include `<read_first>`, `<action>`, `<acceptance_criteria>`. Commit identity = Matt-Aurora-Ventures <lucidbloks@gmail.com>. No Co-Authored-By: Claude. No KuCoin references anywhere. Windows paths with forward slashes in config (Node handles both); use `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/...` form in file paths.
</deep_work_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Root scaffolding — pnpm workspace, Turborepo, TypeScript strict, Biome, Node 22 pin</name>
  <files>
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/package.json
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/pnpm-workspace.yaml
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/turbo.json
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/tsconfig.base.json
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/biome.json
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.nvmrc
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.env.example
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitignore
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/vitest.workspace.ts
  </files>
  <read_first>
    - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/phases/01-foundation/01-RESEARCH.md (Code Examples 1-7 are the exact file shapes to produce)
    - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitignore (check existing contents before overwriting — must preserve any prior entries)
    - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/research/STACK.md (versions lock: Node 22 LTS, pnpm 9.12+, turbo 2.1+, TS 5.7, biome 2.3+, vitest 2.1+)
  </read_first>
  <action>
**Step 1. Install pnpm globally via PowerShell (not bash — bash is broken on this machine):**

In PowerShell: `npm install -g pnpm@9`. Confirm with `pnpm --version` (expect `9.x`).

**Step 2. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.nvmrc` with exactly:**
```
22
```

**Step 3. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/package.json`:**
```json
{
  "name": "kr8tiv-mexc-bot",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22.0.0 <23.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "turbo dev --filter=core",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "smoke": "turbo smoke --filter=core",
    "setup:credentials": "tsx scripts/setup-credentials.ts",
    "verify-env": "tsx scripts/verify-env.ts",
    "preflight": "powershell -ExecutionPolicy Bypass -File scripts/preflight-windows.ps1",
    "prepare": "lefthook install"
  }
}
```

**Step 4. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/pnpm-workspace.yaml`:**
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

**Step 5. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/turbo.json`** exactly per 01-RESEARCH.md Code Example 2 (contains `$schema`, `globalDependencies: ["**/.env.example", "tsconfig.base.json", "biome.json"]`, and tasks `build` (dependsOn ["^build"], outputs ["dist/**"]), `dev` (cache:false, persistent:true), `typecheck` (dependsOn ["^typecheck"]), `test` (dependsOn ["^build"], outputs ["coverage/**"]), `lint`, and `smoke` (cache:false, persistent:false)).

**Step 6. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/tsconfig.base.json`** exactly per 01-RESEARCH.md Code Example 3 — `target: "ES2023"`, `module: "NodeNext"`, `moduleResolution: "NodeNext"`, `strict: true`, `noImplicitAny: true`, `strictNullChecks: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`, `isolatedModules: true`, `verbatimModuleSyntax: true`, `skipLibCheck: true`, `declaration: true`, `declarationMap: true`, `sourceMap: true`, `incremental: true`.

**Step 7. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/biome.json`** exactly per 01-RESEARCH.md Code Example 5 — the CRITICAL lines are `"lineEnding": "crlf"` (Windows stability) and `"suspicious": { "noExplicitAny": "warn", "noConsoleLog": "error" }` (stops accidental console.log of secrets, Pitfall 2 from RESEARCH.md).

**Step 8. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.env.example`** exactly per 01-RESEARCH.md Code Example 7 — sets `NODE_ENV=development`, `LOG_LEVEL=info`, `LOG_PRETTY=true`, `MEXC_SPOT_BASE_URL=https://api.mexc.com`, `MEXC_FUTURES_BASE_URL=https://contract.mexc.com`, `MEXC_RECV_WINDOW_MS=5000`, `REDIS_URL=redis://127.0.0.1:6379`, `SQLITE_PATH=./data/core.sqlite`. Add a header comment: "# All secrets are in Windows Credential Manager — do NOT put keys here."

**Step 9. Append to `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitignore`** (preserve any existing entries, add these):
```
node_modules/
.turbo/
dist/
coverage/
data/
.env.local
.env.*.local
*.log
.DS_Store
Thumbs.db
```

**Step 10. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/vitest.workspace.ts`:**
```typescript
export default [
  "apps/*/vitest.config.ts",
  "packages/*/vitest.config.ts",
];
```

**Step 11. Install root devDependencies via PowerShell:**
```powershell
cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot
pnpm add -D -w typescript@^5.7 tsx@^4.19 tsup@^8.3 vitest@^2.1 `@vitest/coverage-v8`@^2.1 `@biomejs/biome`@^2.3 `@types/node`@^22 turbo@^2.1 lefthook@^1.8
```
(Note the backticks in PowerShell for literal `@` in package names. In actual execution use `pnpm add -D -w typescript@^5.7 tsx@^4.19 tsup@^8.3 vitest@^2.1 @vitest/coverage-v8@^2.1 @biomejs/biome@^2.3 @types/node@^22 turbo@^2.1 lefthook@^1.8` — PowerShell handles this fine without backticks when packages aren't starting with @.)

**Step 12. Verify typecheck infrastructure:** Run `pnpm turbo typecheck --dry=json` from `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/`. It should list zero tasks (no workspace packages have typecheck scripts yet) and exit 0. This confirms turbo.json is valid.

**Step 13. Do NOT add commit identity here** — identity is enforced via lefthook + operator discipline (CLAUDE.md mandate). Do NOT run `git config user.name Claude` anywhere; if git config must be touched, it is `Matt-Aurora-Ventures <lucidbloks@gmail.com>` and only at repo scope via `git config --local user.name "Matt-Aurora-Ventures"` + `git config --local user.email "lucidbloks@gmail.com"`.
  </action>
  <verify>
    <automated>cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot && pnpm turbo typecheck --dry=json</automated>
  </verify>
  <acceptance_criteria>
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/package.json` exists and contains string `"pnpm@9"` (packageManager field)
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/package.json` contains string `">=22.0.0 <23.0.0"` (engines.node)
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/tsconfig.base.json` contains string `"noUncheckedIndexedAccess": true`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/tsconfig.base.json` contains string `"strict": true`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/biome.json` contains string `"lineEnding": "crlf"`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/biome.json` contains string `"noConsoleLog": "error"`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/pnpm-workspace.yaml` contains both `apps/*` and `packages/*`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/turbo.json` contains task names: `build`, `dev`, `typecheck`, `test`, `lint`, `smoke`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.nvmrc` contents is exactly `22` (optional trailing newline)
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.env.example` does NOT contain any string matching `mx0[A-Za-z0-9]{15,}` (no real keys)
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/node_modules/.bin/turbo` exists (pnpm install succeeded)
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/node_modules/.bin/biome` exists
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/node_modules/.bin/lefthook.exe` or equivalent lefthook binary exists
    - Running `pnpm turbo typecheck --dry=json` from repo root exits 0
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitignore` contains `node_modules/` and `.env.local`
  </acceptance_criteria>
  <done>
    Monorepo boots: pnpm workspace declared, Turborepo task graph valid, TS strict base config in place, Biome configured with CRLF+noConsoleLog, Node 22 pinned, root devDependencies installed, vitest workspace file ready for packages to register. No packages exist yet (Task 2 creates the first two scaffolds).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Package scaffolds (shared-types, shared-schemas) + preflight PowerShell probe</name>
  <files>
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/package.json
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/tsconfig.json
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/src/index.ts
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/package.json
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/tsconfig.json
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/src/index.ts
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/scripts/preflight-windows.ps1
  </files>
  <read_first>
    - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/phases/01-foundation/01-RESEARCH.md (Architecture Patterns section — SecretName union; Environment Availability section — preflight probe requirements)
    - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/tsconfig.base.json (created in Task 1 — the per-package tsconfig extends this)
    - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/package.json (Task 1 output — confirm packageManager value)
  </read_first>
  <action>
**Step 1. Create `packages/shared-types/package.json`** (the package name `@kr8tiv/shared-types` — this namespace is used by all internal packages for clean imports):
```json
{
  "name": "@kr8tiv/shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "workspace:*"
  }
}
```
Note: The `"typescript": "workspace:*"` pattern won't work unless TypeScript is declared at root as a workspace dep — which it IS (Task 1 installed typescript at root with `-w`). Verify by checking `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/package.json` has `"typescript"` under `devDependencies`. If workspace:* fails, use `"typescript": "^5.7"` instead.

**Step 2. Create `packages/shared-types/tsconfig.json`:**
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "noEmit": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
```

**Step 3. Create `packages/shared-types/src/index.ts`** (the SecretName allow-list is load-bearing — downstream plans 02, 03, 04 import it):
```typescript
/**
 * Allow-list union of known Windows Credential Manager secret names for
 * kr8tiv-mexc-bot. Every secret the app ever reads MUST appear here.
 *
 * Naming convention: lowercase kebab-case; used as the final path component
 * of the Credential Manager service name `kr8tiv-mexc-bot/<name>`.
 *
 * Phase 1: mexc-spot-access, mexc-spot-secret, mexc-whitelist-ip (all 3 already provisioned)
 * Phase 3 adds: telegram-bot-token
 * Phase 6 adds: mexc-futures-access, mexc-futures-secret
 */
export type SecretName =
  | "mexc-spot-access"
  | "mexc-spot-secret"
  | "mexc-whitelist-ip"
  | "telegram-bot-token"
  | "mexc-futures-access"
  | "mexc-futures-secret";

/**
 * Branded primitive type representing a secret value. Prevents accidental
 * leakage through logging or serialization — unwrapping requires an explicit
 * `unsafeReveal()` call (grep-able).
 *
 * Implementation in packages/secrets is the only place that mints these.
 */
declare const SecretBrand: unique symbol;
export type Secret<T extends string = string> = T & { readonly [SecretBrand]: true };
```

**Step 4. Create `packages/shared-schemas/package.json`:**
```json
{
  "name": "@kr8tiv/shared-schemas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23"
  },
  "devDependencies": {
    "typescript": "workspace:*"
  }
}
```

**Step 5. Create `packages/shared-schemas/tsconfig.json`** (identical shape to shared-types tsconfig).

**Step 6. Create `packages/shared-schemas/src/index.ts`** (empty re-export barrel — plans 02/03/04 add schemas here):
```typescript
/**
 * Shared Zod runtime schemas for kr8tiv-mexc-bot.
 *
 * Phase 1 plan 01-01 creates this empty barrel. Downstream plans add:
 * - plan 01-02: SecretName validator (redundant with shared-types but available at runtime)
 * - plan 01-04: MEXC spot + futures response schemas (ping, balance, etc.)
 *
 * Do NOT add any schemas here in plan 01-01. This is a scaffold file only.
 */
export {};
```

**Step 7. Create `scripts/preflight-windows.ps1`:**
```powershell
# scripts/preflight-windows.ps1
# Invoked via: pnpm preflight
# Probes all Windows-side prerequisites for Phase 1 and prints a checklist
# with install commands for any missing item.

$ErrorActionPreference = "Continue"
$failures = @()

function Check-Cmd($name, $minVersion, $installCmd) {
  $cmd = Get-Command $name -ErrorAction SilentlyContinue
  if ($null -eq $cmd) {
    Write-Host "[MISSING] $name" -ForegroundColor Red
    Write-Host "          Install: $installCmd"
    return $false
  }
  Write-Host "[OK]      $name found at $($cmd.Source)" -ForegroundColor Green
  return $true
}

Write-Host "`n=== kr8tiv-mexc-bot Windows preflight ===`n" -ForegroundColor Cyan

if (-not (Check-Cmd "node" "22" "winget install OpenJS.NodeJS.LTS")) { $failures += "node" }
else {
  $nodeVer = (& node --version) -replace '^v', ''
  $major = [int]($nodeVer -split '\.')[0]
  if ($major -lt 22 -or $major -ge 23) {
    Write-Host "[WARN]    node version $nodeVer is NOT 22.x (required)" -ForegroundColor Yellow
    $failures += "node-version"
  } else {
    Write-Host "          node version $nodeVer (OK)"
  }
}

if (-not (Check-Cmd "pnpm" "9" "npm install -g pnpm@9")) { $failures += "pnpm" }
if (-not (Check-Cmd "gitleaks" "8.24" "winget install gitleaks.gitleaks")) { $failures += "gitleaks" }
if (-not (Check-Cmd "git" "2" "winget install Git.Git")) { $failures += "git" }

# Memurai (Redis) — check Windows service
$memurai = Get-Service -Name "Memurai" -ErrorAction SilentlyContinue
if ($null -eq $memurai) {
  Write-Host "[MISSING] Memurai service" -ForegroundColor Red
  Write-Host "          Install: winget install MemuraiDeveloper"
  $failures += "memurai"
} elseif ($memurai.Status -ne "Running") {
  Write-Host "[WARN]    Memurai service present but not Running (current: $($memurai.Status))" -ForegroundColor Yellow
  Write-Host "          Start:   Start-Service Memurai"
  $failures += "memurai-stopped"
} else {
  Write-Host "[OK]      Memurai service Running" -ForegroundColor Green
}

# Windows Credential Manager targets (cannot read values — only existence)
# We can't probe WCM without a native binding. Just remind the operator.
Write-Host "`n-- Windows Credential Manager --"
Write-Host "Required targets (verify manually or via setup-credentials script after install):"
Write-Host "  - kr8tiv-mexc-bot/mexc-spot-access"
Write-Host "  - kr8tiv-mexc-bot/mexc-spot-secret"
Write-Host "  - kr8tiv-mexc-bot/mexc-whitelist-ip"

if ($failures.Count -gt 0) {
  Write-Host "`n=== $($failures.Count) prerequisite(s) missing ===`n" -ForegroundColor Red
  Write-Host "Fix the items above, then re-run: pnpm preflight"
  exit 1
}

Write-Host "`n=== All preflight checks passed ===`n" -ForegroundColor Green
exit 0
```

**Step 8. Run `pnpm install` in repo root** to register the new workspace packages.

**Step 9. Run `pnpm turbo typecheck`** from repo root. It should now run `tsc --noEmit` on both packages and pass with zero errors.
  </action>
  <verify>
    <automated>cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot && pnpm install && pnpm turbo typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/src/index.ts` contains string `SecretName` and union member `"mexc-spot-access"`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/src/index.ts` contains `Secret<T extends string = string>` brand type
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-types/package.json` name is `@kr8tiv/shared-types`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/src/index.ts` contains `export {};`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/package.json` name is `@kr8tiv/shared-schemas`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/packages/shared-schemas/package.json` declares `zod` dependency
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/scripts/preflight-windows.ps1` exists and contains string `Memurai`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/scripts/preflight-windows.ps1` contains the three required secret target names (mexc-spot-access, mexc-spot-secret, mexc-whitelist-ip)
    - `pnpm turbo typecheck` from repo root exits 0 with 2 successful tasks (shared-types, shared-schemas)
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/node_modules/@kr8tiv/shared-types` symlink/dir exists (pnpm workspace linked the package)
  </acceptance_criteria>
  <done>
    Two skeleton packages exist in the workspace, both typecheck cleanly against the strict base config. `SecretName` allow-list is the source of truth for downstream plans. Preflight PowerShell script probes Node 22, pnpm, gitleaks, git, and Memurai service status — emits actionable install commands on failure.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: lefthook + gitleaks — Windows-safe pre-commit with custom MEXC/Telegram rules</name>
  <files>
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/lefthook.yml
    C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitleaks.toml
  </files>
  <read_first>
    - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/phases/01-foundation/01-RESEARCH.md (Common Pitfalls → Pitfall 5 "Broken Git Bash" and Pitfall 6 "gitleaks MEXC rules" — the TOML + YAML contents are prescriptive)
    - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/research/PITFALLS.md (Pitfall 10 Secrets leakage — why three layers of defense matter)
    - C:/Users/lucid/Desktop/kr8tiv-mexc-bot/package.json (confirm lefthook is in devDependencies from Task 1)
  </read_first>
  <action>
**Step 1. Install gitleaks via winget in PowerShell:**
```powershell
winget install gitleaks.gitleaks
```
Verify with `gitleaks version` (expect `8.24+` or later). If winget is unavailable or blocked, download the binary directly from https://github.com/gitleaks/gitleaks/releases/latest (pick `gitleaks_8.x.x_windows_x64.zip`), extract `gitleaks.exe` to a directory in `PATH` (e.g., `C:/Users/lucid/bin/`).

**Step 2. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/lefthook.yml`:**
```yaml
# lefthook.yml
# Rust-binary pre-commit hooks. Survives broken Git Bash (no shell dependency).
# Run `pnpm prepare` or `pnpm exec lefthook install` after clone to wire .git/hooks.

pre-commit:
  parallel: true
  commands:
    gitleaks:
      run: gitleaks protect --staged --verbose --redact --config=.gitleaks.toml
      fail_text: "Secret detected in staged changes. Commit blocked. Review output above."
    biome-check:
      glob: "*.{ts,tsx,js,jsx,json,jsonc,md}"
      run: pnpm biome check --no-errors-on-unmatched {staged_files}
      fail_text: "Biome lint/format errors. Run `pnpm lint:fix` to auto-fix."
    typecheck:
      glob: "*.{ts,tsx}"
      run: pnpm turbo typecheck
      fail_text: "TypeScript errors in workspace. Fix before committing."

pre-push:
  commands:
    gitleaks-full:
      run: gitleaks detect --verbose --redact --config=.gitleaks.toml
      fail_text: "Secret detected in history. Push blocked."
```

**Step 3. Write `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitleaks.toml`** EXACTLY per 01-RESEARCH.md Common Pitfalls → Pitfall 6. Contents (verbatim):

```toml
# .gitleaks.toml
# Extend the default ruleset with MEXC + Telegram patterns.

title = "kr8tiv-mexc-bot gitleaks config"

[extend]
useDefault = true

# ---- MEXC Access Key ----
# Format observed: "mx0" + alphanumeric, typically 18-40 chars total
[[rules]]
id          = "mexc-access-key"
description = "MEXC API access key (mx0 prefix)"
regex       = '''mx0[A-Za-z0-9]{15,40}'''
secretGroup = 0
entropy     = 3.5
keywords    = ["mx0"]
tags        = ["api-key", "mexc"]

# ---- MEXC Secret Key ----
# 32-character hex string; high false-positive rate so REQUIRE keyword context
[[rules]]
id          = "mexc-secret-key"
description = "MEXC API secret key (32-char hex) - keyword-gated"
regex       = '''(?i)(mexc[_-]?secret|mexc[_-]?api[_-]?secret|MEXC_SPOT_SECRET|MEXC_FUTURES_SECRET)\s*[=:"']+\s*([a-f0-9]{32})\b'''
secretGroup = 2
entropy     = 3.0
keywords    = ["mexc"]
tags        = ["secret", "mexc"]

# ---- Telegram Bot Token ----
# Format: <digits>:<35-char-mixed>  e.g. 123456789:AAH2_aBcDeF...
[[rules]]
id          = "telegram-bot-token"
description = "Telegram Bot API token"
regex       = '''[0-9]{9,10}:[A-Za-z0-9_-]{35}'''
secretGroup = 0
entropy     = 3.5
keywords    = ["telegram", "bot"]
tags        = ["telegram", "bot-token"]

# ---- Ethereum private key (scaffold for Phase 7) ----
[[rules]]
id          = "eth-private-key"
description = "Ethereum private key (64-char hex)"
regex       = '''(?i)(private[_-]?key|priv[_-]?key|eth[_-]?key)\s*[=:"']+\s*(0x)?([a-f0-9]{64})\b'''
secretGroup = 3
entropy     = 3.5
keywords    = ["private", "key", "eth"]
tags        = ["wallet", "private-key"]

# ---- Solana private key (scaffold for Phase 7) ----
[[rules]]
id          = "solana-private-key"
description = "Solana private key (base58, 87-88 chars) - keyword-gated"
regex       = '''(?i)(solana[_-]?key|phantom[_-]?key|sol[_-]?priv)\s*[=:"']+\s*([1-9A-HJ-NP-Za-km-z]{86,90})'''
secretGroup = 2
entropy     = 4.0
keywords    = ["solana", "phantom"]
tags        = ["wallet", "private-key", "solana"]

# ---- Allowlist ----
[allowlist]
description = "Paths to exclude from scanning"
paths = [
  '''\.planning/.*\.md''',
  '''\.gitleaks\.toml''',
  '''package-lock\.json''',
  '''pnpm-lock\.yaml''',
]
commits = []
regexes = [
  '''mx0npKfh57kEEVmyLa''',
  '''mx0aBYs33eIilxBWC5''',
  '''51f38875ebe0475dad6236783a95cc19''',
]
```

**Step 4. Wire lefthook into .git/hooks:** In PowerShell from repo root:
```powershell
cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot
pnpm exec lefthook install
```
This rewrites `.git/hooks/pre-commit` and `.git/hooks/pre-push` to invoke lefthook. Verify by reading `.git/hooks/pre-commit` — it should reference `lefthook` (NOT a bash shebang calling husky).

**Step 5. Manual verification of gitleaks rule (execute this test and confirm failure):**

Create a temp file (DO NOT COMMIT):
```
C:/Users/lucid/Desktop/kr8tiv-mexc-bot/TEMP-LEAK-TEST.txt
```
With contents:
```
MEXC_SPOT_ACCESS=mx0testkeyabcdef0123456789abcdef
```
Run:
```powershell
cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot
git add TEMP-LEAK-TEST.txt
gitleaks protect --staged --verbose --redact --config=.gitleaks.toml
```
Expected: gitleaks exits non-zero with a "mexc-access-key" finding. Then: `git reset HEAD TEMP-LEAK-TEST.txt` to unstage, and delete `TEMP-LEAK-TEST.txt` before proceeding.

**Step 6. Also verify the Telegram token rule:**

Create a temp file with contents:
```
TELEGRAM_BOT_TOKEN=1234567890:AABBCCDDEEFFGGHHIIJJKKLLMMNNOOPPQQR
```
Repeat the git add + gitleaks protect dance. Expected: non-zero exit, `telegram-bot-token` finding. Clean up after.

**Step 7. Set local git commit identity** — CLAUDE.md mandates commits as Matt-Aurora-Ventures. In PowerShell from repo root:
```powershell
cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot
git config --local user.name "Matt-Aurora-Ventures"
git config --local user.email "lucidbloks@gmail.com"
```
Verify with `git config --local user.name` (expect `Matt-Aurora-Ventures`) and `git config --local user.email` (expect `lucidbloks@gmail.com`).
  </action>
  <verify>
    <automated>cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot && gitleaks detect --no-git --verbose --config=.gitleaks.toml --source=. --exit-code=0</automated>
  </verify>
  <acceptance_criteria>
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitleaks.toml` exists and contains rule id `mexc-access-key`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitleaks.toml` contains rule id `telegram-bot-token`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.gitleaks.toml` contains rule id `mexc-secret-key`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/lefthook.yml` exists and contains string `gitleaks protect --staged`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/lefthook.yml` contains string `typecheck`
    - `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.git/hooks/pre-commit` exists and references `lefthook` (NOT `husky`, NOT a bash `#!/bin/sh` that imports from `.husky/`)
    - Manual test: Creating a file containing string `mx0testkeyabcdef0123456789abcdef` and running `gitleaks protect --staged --config=.gitleaks.toml` after staging it exits non-zero with finding id `mexc-access-key`
    - Manual test: Creating a file containing a Telegram bot token pattern `1234567890:AABBCCDDEEFFGGHHIIJJKKLLMMNNOOPPQQR` and running gitleaks after staging exits non-zero with finding id `telegram-bot-token`
    - `git config --local user.name` returns exactly `Matt-Aurora-Ventures`
    - `git config --local user.email` returns exactly `lucidbloks@gmail.com`
    - Running `gitleaks detect --no-git --config=.gitleaks.toml --source=C:/Users/lucid/Desktop/kr8tiv-mexc-bot/ --exit-code=0` (full scan of working tree, exit-code=0 means "don't fail on findings, just scan") does not crash or fail to parse the TOML
    - Grep confirms NO husky references anywhere: searching for `husky` in the repo (excluding `.git/` and `node_modules/`) returns zero matches
  </acceptance_criteria>
  <done>
    Pre-commit pipeline is wired with three defense layers: (1) gitleaks blocks commits containing MEXC mx0 keys, MEXC hex secrets, Telegram bot tokens, ETH private keys, Solana private keys — verified by planting a test key and confirming rejection; (2) biome lint+format blocks TypeScript-quality regressions; (3) turbo typecheck blocks type errors. Lefthook is the runner (Rust binary, no shell) — Matt's broken Git Bash cannot silently bypass the hook. Git commit identity is Matt-Aurora-Ventures.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification commands:**

1. **Strict TS monorepo boots:**
   ```
   cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot && pnpm turbo typecheck
   ```
   Must exit 0 with ≥2 successful tasks (shared-types, shared-schemas).

2. **Biome config valid + enforces CRLF + noConsoleLog:**
   ```
   cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot && pnpm biome check --diagnostic-level=error .
   ```
   Must exit 0 (no violations in our files).

3. **gitleaks config parses and scans the whole tree:**
   ```
   cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot && gitleaks detect --no-git --config=.gitleaks.toml --source=. --exit-code=0
   ```
   Must exit 0 (no findings in our own repo since we allowlisted the research-doc example keys).

4. **Pre-commit hook fires:**
   Create `TEMP-LEAK-TEST.txt` containing `mx0testkeyabcdef0123456789abcdef`, `git add`, attempt `git commit -m "test"`. Expected: commit rejected by gitleaks with `mexc-access-key` finding. Clean up after.

5. **Commit identity locked:**
   ```
   cd C:/Users/lucid/Desktop/kr8tiv-mexc-bot && git config --local user.name && git config --local user.email
   ```
   Must return `Matt-Aurora-Ventures` and `lucidbloks@gmail.com`.

6. **No husky footprint:**
   Grep for `husky` in repo (excluding `.git/` and `node_modules/`). Zero matches.

7. **Preflight script runs:**
   ```
   pnpm preflight
   ```
   Must execute PowerShell and emit the status table. (May exit non-zero if Memurai isn't installed yet — that's handled in plan 01-03.)
</verification>

<success_criteria>
- [ ] FND-01 satisfied: pnpm workspaces + Turborepo monorepo exists with Node 22 LTS engines pin and TypeScript 5.5+ (5.7) strict via `tsconfig.base.json` with `strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`. `pnpm turbo typecheck` green.
- [ ] FND-10 satisfied: lefthook + gitleaks pre-commit hook installed; `.gitleaks.toml` has custom MEXC `mx0` access-key rule + MEXC 32-hex secret rule + Telegram bot-token rule + Ethereum/Solana private-key scaffolds; `.git/hooks/pre-commit` invokes lefthook binary (not husky/bash); planting a test `mx0...` string in a staged file causes commit rejection.
- [ ] Biome configured with `lineEnding: "crlf"` (Windows stability) and `noConsoleLog: "error"` (Pitfall 2 defense).
- [ ] Two foundational packages (`@kr8tiv/shared-types`, `@kr8tiv/shared-schemas`) typecheck cleanly — downstream plans consume `SecretName` and `Secret<T>` from shared-types.
- [ ] Preflight PowerShell script probes Node 22, pnpm 9, gitleaks, git, and Memurai service; enumerates required Windows Credential Manager targets for operator reference.
- [ ] Git commit identity set to Matt-Aurora-Ventures `<lucidbloks@gmail.com>` — no Co-Authored-By lines will appear in any commit this plan makes.
- [ ] Zero KuCoin references (grep confirms).
- [ ] Zero husky references (grep confirms — lefthook only).
</success_criteria>

<output>
After completion, create `C:/Users/lucid/Desktop/kr8tiv-mexc-bot/.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Root config files created (exact paths)
- Two packages scaffolded (@kr8tiv/shared-types, @kr8tiv/shared-schemas) with their public exports
- Lefthook wiring (.git/hooks/pre-commit → lefthook binary) and gitleaks custom rules installed
- Preflight probe script and how operator runs it (`pnpm preflight`)
- Git commit identity configured to Matt-Aurora-Ventures
- Any deviations from RESEARCH.md examples (ideally: none)
</output>